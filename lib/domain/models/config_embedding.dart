/// class to configure the embedding configurations
class ConfigEmbedding {
  /// model that will be used to make the embedding
  final String model;

  ConfigEmbedding({this.model = "text-embedding-ada-002"});
}
